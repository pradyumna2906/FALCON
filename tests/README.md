# Cross-Component Tests

This directory contains contract, integration and end-to-end tests spanning multiple FALCON components.

Component-level unit tests should remain close to their backend, frontend or ML source code. Tests must use synthetic data and must not depend on real credentials or personal records.
