# Backend

This directory contains the FastAPI application and its server-side modules.

The backend is authoritative for authentication, authorization, validation, financial calculations, persistence and orchestration. It must never expose secrets or trust client-calculated financial values.

Backend implementation begins in Phase 1.
