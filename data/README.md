# Data

This directory organizes development data assets.

Only synthetic or explicitly approved de-identified data may be versioned. Raw imports, processed user data and uploads are local or generated assets and must never be committed.
